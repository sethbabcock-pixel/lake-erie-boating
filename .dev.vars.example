# Local dev secrets for `wrangler dev` (Cloudflare Workers). Copy to .dev.vars and fill in.
# Resend API key for transactional email (password reset, admin notices).
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx
